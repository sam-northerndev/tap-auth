import React from "react"
import { Link } from "gatsby"

const Login = () => (
  <div>
    <h1>This will contain the login form</h1>
    <Link to="/">Go back to the homepage</Link>
  </div>
)

export default Login
